name: Sync Fork

on:
  schedule:
  - cron: '21 9 * * SUN'

jobs:
  sync:

    runs-on: ubuntu-latest
    
    steps:
    - uses: TG908/fork-sync@v1.1.7
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        owner: WillKoehrsen
